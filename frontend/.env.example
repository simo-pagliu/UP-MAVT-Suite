# Base URL of the backend API.
# Override this in .env.local or at build time for production deployments.
VITE_API_URL=http://localhost:5000/api

# Enable browser-side debug logging of failed API calls and uncaught errors.
# Keep this false in production unless you are actively troubleshooting.
VITE_DEBUG_CONSOLE=false
